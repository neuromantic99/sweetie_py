vel = im.date_2018_03_26.area1.session_behaviour.velocity;
flu = im.date_2018_03_26.area1.plane1.fluoresence_corrected;
lick = im.date_2018_03_26.area1.session_behaviour.lick_event;
avflu = mean(flu);

% avflu = smooth(avflu);
% vel = smooth(vel);

avflu = avflu(1:length(vel));




close all, figure
subplot(2,1,1)
plot(avflu)
title('average fluoresence')
subplot(2,1,2)

plot(vel)
title('velocity')
